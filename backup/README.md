# MariaDB Backup and Recovery

This directory contains utilities for MariaDB backup, backup verification, backup retention management, and restore validation.

## Utilities

### backup.sh

Creates a logical MariaDB backup using `mysqldump`.

Example:

```bash
./backup.sh
